/**
 * Dashboard module — desk overview.
 *
 *   import DashboardModule from '../components/ws/modules/dashboard';
 *
 * Controlled: the host supplies `symbol` / `strategyId` and receives the
 * matching onSelect callbacks, so the workspace store stays authoritative.
 */
export { DashboardModule, StrategyCard, default } from './DashboardModule';
