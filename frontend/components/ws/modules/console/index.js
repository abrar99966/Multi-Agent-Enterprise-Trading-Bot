/**
 * Console dock — the bottom monitoring surface.
 *
 *   import ConsoleDock from '../components/ws/modules/console';
 *
 * Controlled: the host passes the workspace activity log and the symbol
 * selector, so row clicks re-scope the rest of the workspace.
 */
export { ConsoleDock, default } from './ConsoleDock';
